<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> - CollabSync
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/i_index/rankings.css">
</head>

<body>
    <nav class="navbar navbar-light">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/customer"><i class="fas fa-shopping-bag me-2"></i>CollabSync</a>
            <ul class="nav me-auto">
                <li class="nav-item"><a class="nav-link" href="/customer">All Campaigns</a></li>
                <li class="nav-item"><a class="nav-link active" href="/customer/rankings">Rankings</a></li>
            </ul>
            <a class="btn btn-primary" href="/customer/cart"><i class="fas fa-shopping-cart me-2"></i>Cart</a>
            <form class="d-flex align-items-center gap-2 filters-wrap" method="get" action="/customer/rankings">
                <div class="d-flex align-items-center me-2">
                    <label class="me-2">Brand:</label>
                    <select class="form-select me-3" name="brandCategory" onchange="this.form.submit()">
                        <option value="revenue" <%=brandCategory==='revenue' ? 'selected' : '' %>>Campaign Revenue
                        </option>
                        <option value="engagement_rate" <%=brandCategory==='engagement_rate' ? 'selected' : '' %>
                            >Engagement Rate</option>
                        <option value="rating" <%=brandCategory==='rating' ? 'selected' : '' %>>Rating</option>
                        <option value="completedCampaigns" <%=brandCategory==='completedCampaigns' ? 'selected' : '' %>
                            >Completed Campaigns</option>
                    </select>
                </div>
                <div class="d-flex align-items-center me-2">
                    <label class="me-2">Influencer:</label>
                    <select class="form-select me-3" name="influencerCategory" onchange="this.form.submit()">
                        <option value="totalFollowers" <%=influencerCategory==='totalFollowers' ? 'selected' : '' %>
                            >Total Followers</option>
                        <option value="engagement_rate" <%=influencerCategory==='engagement_rate' ? 'selected' : '' %>
                            >Engagement Rate</option>
                        <option value="platform_count" <%=influencerCategory==='platform_count' ? 'selected' : '' %>
                            >Platform Count</option>
                        <option value="completedCampaigns" <%=influencerCategory==='completedCampaigns' ? 'selected'
                            : '' %>>Completed Campaigns</option>
                    </select>
                </div>
            </form>
        </div>
    </nav>

    <div class="container my-4 rankings-container">
        <div class="row">
            <div class="col-lg-6">
                <h3 class="rankings-section-title mb-1">
                    <i class="fas fa-trophy text-warning"></i>
                    Top Brands
                </h3>
                <small class="rankings-subtitle">Sorted by:
                    <%= brandCategory==='revenue' ? 'Campaign Revenue' : brandCategory==='engagement_rate'
                        ? 'Engagement Rate' : brandCategory==='rating' ? 'Rating' : 'Completed Campaigns' %>
                </small>
                <ol class="list-group list-group-numbered">
                    <% brandRankings.forEach(b=> { %>
                        <li class="list-group-item d-flex align-items-center">
                            <img src="<%= b.logoUrl %>" alt="<%= b.name %>" class="me-3 ranking-avatar"
                                onerror="this.style.display='none'">
                            <div class="flex-grow-1">
                                <div class="rank-name">
                                    <%= b.name %>
                                </div>
                                <% let brandMetricLabel='' ; let brandMetricValue='' ; if (brandCategory==='revenue' ) {
                                    brandMetricLabel='Revenue' ; brandMetricValue='$' +(b.revenue||0).toLocaleString();
                                    } else if (brandCategory==='engagement_rate' ) { brandMetricLabel='Engagement Rate'
                                    ; brandMetricValue=(b.engagement_rate||0).toFixed(2)+'%'; } else if
                                    (brandCategory==='rating' ) { brandMetricLabel='Rating' ;
                                    brandMetricValue=(b.rating||0).toFixed(2)+'/5'; } else {
                                    brandMetricLabel='Completed Campaigns' ; brandMetricValue=(b.completedCampaigns||0);
                                    } %>
                                    <div class="rank-meta">
                                        <span class="metric-chip">
                                            <i class="fas fa-chart-line"></i>
                                            <%= brandMetricLabel %>: <%= brandMetricValue %>
                                        </span>
                                    </div>
                            </div>
                        </li>
                        <% }) %>
                </ol>
            </div>
            <div class="col-lg-6">
                <h3 class="rankings-section-title mb-1">
                    <i class="fas fa-star text-warning"></i>
                    Top Influencers
                </h3>
                <small class="rankings-subtitle">Sorted by:
                    <%= influencerCategory==='totalFollowers' ? 'Total Followers' :
                        influencerCategory==='engagement_rate' ? 'Engagement Rate' :
                        influencerCategory==='platform_count' ? 'Platform Count' : 'Completed Campaigns' %>
                </small>
                <ol class="list-group list-group-numbered">
                    <% influencerRankings.forEach(i=> { %>
                        <li class="list-group-item d-flex align-items-center">
                            <img src="<%= i.profilePicUrl %>" alt="<%= i.name %>" class="me-3 ranking-avatar"
                                onerror="this.src='/images/default-avatar.jpg'">
                            <div class="flex-grow-1">
                                <div class="rank-name">
                                    <%= i.name %>
                                </div>
                                <% let infMetricLabel='' ; let infMetricValue='' ; if
                                    (influencerCategory==='totalFollowers' ) { infMetricLabel='Total Followers' ;
                                    infMetricValue=(i.totalFollowers||0).toLocaleString(); } else if
                                    (influencerCategory==='engagement_rate' ) { infMetricLabel='Engagement Rate' ;
                                    infMetricValue=(i.engagement_rate||0).toFixed(2)+'%'; } else if
                                    (influencerCategory==='platform_count' ) { infMetricLabel='Platform Count' ;
                                    infMetricValue=(i.platform_count||0); } else { infMetricLabel='Completed Campaigns'
                                    ; infMetricValue=(i.completedCampaigns||0); } %>
                                    <div class="rank-meta">
                                        <span class="metric-chip">
                                            <i class="fas fa-users"></i>
                                            <%= infMetricLabel %>: <%= infMetricValue %>
                                        </span>
                                    </div>
                            </div>
                        </li>
                        <% }) %>
                </ol>
            </div>
        </div>
    </div>
</body>

</html>