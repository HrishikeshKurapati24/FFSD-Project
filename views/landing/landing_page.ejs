<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CollabSync - Bridging Influencers, Brands & Customers</title>
  <link rel="stylesheet" href="/landing/landing_page.css">

</head>

<body>

  <!-- Header -->
  <header>
    <div class="header-container">
      <div class="logo">CollabSync</div>
      <nav>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/Sup_role">Sign Up</a></li>
          <li><a href="/SignIn">Sign In</a></li>
          <li><a href="/about">About</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <!-- Hero Section -->
  <section class="hero">
    <video autoplay muted loop id="heroVideo">
      <source src="/Lp_index/Anima_001.mp4" type="video/mp4">
    </video>
    <div class="hero-content">
      <h1>Bridging Influencers, Brands & Customers</h1>
      <p>Your one-stop platform for authentic connections, strategic collaborations, and sustainable growth in the
        digital ecosystem.</p>
      <a href="/Sup_role" class="btn-hero">Join Us Today</a>
    </div>
  </section>

  <!-- Benefits Section -->
  <section class="benefits">
    <h2 class="section-title">Who We Serve</h2>
    <div class="benefits-container">
      <div class="benefit">
        <div class="benefit-icon">👑</div>
        <h2>For Influencers</h2>
        <p>Showcase your authentic voice and reach to connect with brands that align with your values. Manage campaigns,
          track performance metrics, and grow your professional network all in one place.</p>
        <a href="/influencer/Sup_i" class="button">I'm an Influencer</a>
      </div>
      <div class="benefit">
        <div class="benefit-icon">🏢</div>
        <h2>For Brands</h2>
        <p>Discover creators who truly resonate with your brand values and audience. Launch data-driven campaigns,
          measure ROI with precision, and build lasting relationships with influential voices in your industry.</p>
        <a href="/brand/Sup_b" class="button">I'm a Brand</a>
      </div>
      <div class="benefit">
        <div class="benefit-icon">👥</div>
        <h2>For Customers</h2>
        <p>Access exclusive deals from your favorite brands and influencers. Participate in interactive live sessions,
          read authentic reviews, and enjoy a direct line of communication with the creators you trust.</p>
        <a href="/customer" class="button">I'm a Customer</a>
      </div>
    </div>
  </section>

  <!-- Info Blocks Section -->
  <section class="info-blocks">
    <div class="info-block block-one">
      <h2>Strategic Growth Through Authentic Connections</h2>
      <p>Our data-driven platform helps brands find the perfect influencer match based on audience demographics,
        engagement rates, and content quality. Create more impactful content, measure real campaign results, and scale
        your reach across multiple channels.</p>
      <div class="stats">
        <div class="stat-item">
          <div class="stat-number">93%</div>
          <div class="stat-label">Higher Engagement</div>
        </div>
        <div class="stat-item">
          <div class="stat-number">5.2x</div>
          <div class="stat-label">ROI Increase</div>
        </div>
        <div class="stat-item">
          <div class="stat-number">10K+</div>
          <div class="stat-label">Active Creators</div>
        </div>
      </div>
    </div>
    <div class="info-block block-two">
      <h2>Full-Service Campaign Management</h2>
      <p>Our expert team handles every aspect of your influencer campaigns. From strategy development and creator
        selection to content review, performance tracking, and optimization. We've helped Fortune 500 companies and
        fast-growing e-commerce brands maximize their influencer marketing efforts since 2013.</p>
      <div class="stats">
        <div class="stat-item">
          <div class="stat-number">1.2K+</div>
          <div class="stat-label">Brands Served</div>
        </div>
        <div class="stat-item">
          <div class="stat-number">15K+</div>
          <div class="stat-label">Campaigns Managed</div>
        </div>
        <div class="stat-item">
          <div class="stat-number">48hr</div>
          <div class="stat-label">Average Setup Time</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Three Blocks Section -->
  <section class="three-blocks">
    <div class="block">
      <div class="block-image">
        <img src="/Lp_index/pic4-removebg.png" alt="Influencer Marketing">
      </div>
      <div class="block-text">
        <h2>For Influencers</h2>
        <p>As an influencer on CollabSync, you gain access to a curated marketplace of brand partnerships that align
          with your unique voice and audience. Our platform helps you monetize your content while maintaining
          authenticity and creative control.</p>
        <ul>
          <li>Build a comprehensive profile showcasing your metrics and content style</li>
          <li>Access exclusive brand deals matched to your audience demographics</li>
          <li>Track campaign performance with detailed analytics</li>
          <li>Join a community of like-minded creators for collaboration and growth</li>
        </ul>
      </div>
    </div>
    <div class="block reverse">
      <div class="block-image">
        <img src="/Lp_index/pic66-removebg.png" alt="Brand Partnerships">
      </div>
      <div class="block-text">
        <h2>For Brands</h2>
        <p>CollabSync provides brands with a data-driven approach to influencer marketing. Our platform helps you
          identify the perfect creators based on audience alignment, engagement quality, and proven performance metrics.
        </p>
        <ul>
          <li>Access our network of pre-vetted influencers across all major platforms</li>
          <li>Launch campaigns with streamlined contract management and payments</li>
          <li>Track real-time performance metrics and campaign ROI</li>
          <li>Utilize our AI-powered matching technology for optimal creator partnerships</li>
        </ul>
      </div>
    </div>
    <div class="block">
      <div class="block-image">
        <img src="/Lp_index/pic7.png" alt="Customer Engagement">
      </div>
      <div class="block-text">
        <h2>For Customers</h2>
        <p>As a customer on CollabSync, you're at the heart of a vibrant community connecting you with influencers and
          brands. Enjoy exclusive offers, authentic content, and direct engagement opportunities.</p>
        <ul>
          <li>Access exclusive discounts and promotions from partnered brands</li>
          <li>Participate in live Q&A sessions with your favorite influencers</li>
          <li>Read verified reviews and ratings from the community</li>
          <li>Engage directly with creators for personalized recommendations</li>
        </ul>
      </div>
    </div>
  </section>

  <section class="partners">
    <div class="partners-content">
      <h2 class="section-title">Marketing Integrations and Social Partners</h2>
      <div class="partners-description">
        Collaborate with leading marketing tools and social platforms to amplify your reach and streamline your
        campaigns. Discover top influencers and brands driving authentic connections.
      </div>
      <div class="partners-logos">
        <!-- Retained Partner Logos -->
        <div class="partner-logo"><img src="/Lp_index/Brand_logo_01.png" alt="The Sunrise Shop"></div>
        <div class="partner-logo"><img src="/Lp_index/Brand_logo_02.jpg" alt="BCBP"></div>
        <div class="partner-logo"><img src="/Lp_index/Brand_logo_03.jpg" alt="Horizon"></div>
        <div class="partner-logo"><img src="/Lp_index/Brand_logo_05.jpg" alt="Canyon BBQ & Grill"></div>
        <div class="partner-logo"><img src="/Lp_index/Brand_logo_06.png" alt="Outrun"></div>
      </div>
    </div>
  </section>
  </section>

  <!-- Explore Section -->
  <section class="explore-section">
    <div class="explore-container">
      <h2 class="section-title">Explore Our Platform</h2>
      <p class="explore-description">Discover the amazing brands and influencers who are part of our community</p>
      <div class="explore-buttons">
        <button class="btn-explore" onclick="openBrandsModal()">
          <i class="fas fa-store me-2"></i>View All Brands
        </button>
        <button class="btn-explore" onclick="openInfluencersModal()">
          <i class="fas fa-users me-2"></i>View All Influencers
        </button>
      </div>
    </div>
  </section>

  <!-- Footer  -->
  <footer>
    <div class="footer-logo">CollabSync</div>
    <p>&copy; 2025 CollabSync. All rights reserved.</p>
    <div class="social-media">
      <!-- Currently, these are static images. Once official social media accounts are created, update these to clickable links that redirect to the respective profiles. -->
      <img src="/Lp_index/SocialMedia_logo_i.png" alt="Instagram" />
      <img src="/Lp_index/SocialMedia_logo_y2.png" alt="YouTube" />
      <img src="/Lp_index/SocialMedia_logo_l.png" alt="LinkedIn" />
      <img src="/Lp_index/SocialMedia_logo_f3.jpg" alt="Facebook" />
      <img src="/Lp_index/SocialMedia_logo_t.png" alt="Twitter" />
    </div>
  </footer>

  <!-- Brands Modal -->
  <div id="brandsModal" class="modal">
    <div class="modal-content">
      <div class="modal-header">
        <h2><i class="fas fa-store me-2"></i>All Registered Brands</h2>
        <span class="close" onclick="closeBrandsModal()">&times;</span>
      </div>
      <div class="modal-body">
        <div class="search-container">
          <input type="text" id="brandSearch" placeholder="Search brands..." class="search-input">
        </div>
        <div id="brandsContainer" class="grid-container">
          <!-- Brands will be loaded here -->
        </div>
        <div id="brandsLoading" class="loading">
          <i class="fas fa-spinner fa-spin"></i> Loading brands...
        </div>
      </div>
    </div>
  </div>

  <!-- Influencers Modal -->
  <div id="influencersModal" class="modal">
    <div class="modal-content">
      <div class="modal-header">
        <h2><i class="fas fa-users me-2"></i>All Registered Influencers</h2>
        <span class="close" onclick="closeInfluencersModal()">&times;</span>
      </div>
      <div class="modal-body">
        <div class="search-container">
          <input type="text" id="influencerSearch" placeholder="Search influencers..." class="search-input">
        </div>
        <div id="influencersContainer" class="grid-container">
          <!-- Influencers will be loaded here -->
        </div>
        <div id="influencersLoading" class="loading">
          <i class="fas fa-spinner fa-spin"></i> Loading influencers...
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap CSS for modals and icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">


  <script>
    // Modal functions
    function openBrandsModal() {
      document.getElementById('brandsModal').style.display = 'block';
      loadBrands();
    }

    function closeBrandsModal() {
      document.getElementById('brandsModal').style.display = 'none';
    }

    function openInfluencersModal() {
      document.getElementById('influencersModal').style.display = 'block';
      loadInfluencers();
    }

    function closeInfluencersModal() {
      document.getElementById('influencersModal').style.display = 'none';
    }

    // Close modal when clicking outside
    window.onclick = function (event) {
      const brandsModal = document.getElementById('brandsModal');
      const influencersModal = document.getElementById('influencersModal');

      if (event.target === brandsModal) {
        closeBrandsModal();
      }
      if (event.target === influencersModal) {
        closeInfluencersModal();
      }
    }

    // Load brands data
    async function loadBrands() {
      const container = document.getElementById('brandsContainer');
      const loading = document.getElementById('brandsLoading');

      try {
        loading.style.display = 'block';
        container.innerHTML = '';

        const response = await fetch('/api/brands');
        const brands = await response.json();

        if (brands.length === 0) {
          container.innerHTML = '<div class="text-center py-4"><p>No brands found.</p></div>';
          return;
        }

        brands.forEach(brand => {
          const brandCard = createBrandCard(brand);
          container.appendChild(brandCard);
        });

        // Setup search functionality
        setupBrandSearch(brands);

      } catch (error) {
        console.error('Error loading brands:', error);
        container.innerHTML = '<div class="text-center py-4"><p>Error loading brands. Please try again.</p></div>';
      } finally {
        loading.style.display = 'none';
      }
    }

    // Load influencers data
    async function loadInfluencers() {
      const container = document.getElementById('influencersContainer');
      const loading = document.getElementById('influencersLoading');

      try {
        loading.style.display = 'block';
        container.innerHTML = '';

        const response = await fetch('/api/influencers');
        const influencers = await response.json();

        if (influencers.length === 0) {
          container.innerHTML = '<div class="text-center py-4"><p>No influencers found.</p></div>';
          return;
        }

        influencers.forEach(influencer => {
          const influencerCard = createInfluencerCard(influencer);
          container.appendChild(influencerCard);
        });

        // Setup search functionality
        setupInfluencerSearch(influencers);

      } catch (error) {
        console.error('Error loading influencers:', error);
        container.innerHTML = '<div class="text-center py-4"><p>Error loading influencers. Please try again.</p></div>';
      } finally {
        loading.style.display = 'none';
      }
    }

    // Create brand card
    function createBrandCard(brand) {
      const card = document.createElement('div');
      card.className = 'brand-card';

      const categories = brand.categories ? brand.categories.join(', ') : 'General';

      card.innerHTML = `
        <div class="brand-header">
          <img src="${brand.logoUrl || '/images/default-brand-logo.jpg'}" 
               alt="${brand.brandName}" 
               class="brand-logo"
               onerror="this.src='/images/default-brand-logo.jpg'">
          <div class="brand-info">
            <h3 class="brand-name">${brand.brandName}</h3>
            <p class="brand-industry">${brand.industry || 'General'}</p>
          </div>
        </div>
        
        <div class="stats-grid">
          <div class="stat-item">
            <span class="stat-number">${brand.completedCampaigns || 0}</span>
            <div class="stat-label">Campaigns</div>
          </div>
          <div class="stat-item">
            <span class="stat-number">${brand.influencerPartnerships || 0}</span>
            <div class="stat-label">Completed Partnerships</div>
          </div>
          <div class="stat-item">
            <span class="stat-number">${(brand.avgCampaignRating || 0).toFixed(1)}</span>
            <div class="stat-label">Rating</div>
          </div>
        </div>
        
        <div class="categories">
          <strong>Categories:</strong>
          <div style="margin-top: 0.5rem;">
            ${brand.categories ? brand.categories.map(cat => `<span class="category-tag">${cat}</span>`).join('') : '<span class="category-tag">General</span>'}
          </div>
        </div>
      `;

      return card;
    }

    // Create influencer card
    function createInfluencerCard(influencer) {
      const card = document.createElement('div');
      card.className = 'influencer-card';

      const platformIcons = {
        'instagram': 'fab fa-instagram',
        'youtube': 'fab fa-youtube',
        'tiktok': 'fab fa-tiktok',
        'facebook': 'fab fa-facebook',
        'twitter': 'fab fa-twitter',
        'linkedin': 'fab fa-linkedin'
      };

      const socialPlatforms = influencer.socialPlatforms || [];

      card.innerHTML = `
        <div class="influencer-header">
          <img src="${influencer.profilePicUrl || '/images/default-avatar.jpg'}" 
               alt="${influencer.fullName}" 
               class="influencer-avatar"
               onerror="this.src='/images/default-avatar.jpg'">
          <div class="influencer-info">
            <h3 class="influencer-name">${influencer.fullName}</h3>
            <p class="influencer-niche">${influencer.niche || 'General'}</p>
          </div>
        </div>
        
        <div class="stats-grid">
          <div class="stat-item">
            <span class="stat-number">${(influencer.totalFollowers || 0).toLocaleString()}</span>
            <div class="stat-label">Total Followers</div>
          </div>
          <div class="stat-item">
            <span class="stat-number">${influencer.completedCollabs || 0}</span>
            <div class="stat-label">Completed Collaborations</div>
          </div>
          <div class="stat-item">
            <span class="stat-number">${(influencer.avgRating || 0).toFixed(1)}</span>
            <div class="stat-label">Rating</div>
          </div>
          <div class="stat-item">
            <span class="stat-number">${(influencer.avgEngagementRate || 0).toFixed(1)}%</span>
            <div class="stat-label">Engagement</div>
          </div>
        </div>
        
        <div class="categories">
          <strong>Categories:</strong>
          <div style="margin-top: 0.5rem;">
            ${influencer.categories ? influencer.categories.map(cat => `<span class="category-tag">${cat}</span>`).join('') : '<span class="category-tag">General</span>'}
          </div>
        </div>
        
        ${socialPlatforms.length > 0 ? `
          <div class="categories">
            <strong>Social Platforms:</strong>
            <div class="social-platforms">
              ${socialPlatforms.map(platform => `
                <div class="platform-icon platform-${platform}">
                  <i class="${platformIcons[platform] || 'fas fa-globe'}"></i>
                </div>
              `).join('')}
            </div>
          </div>
        ` : ''}
      `;

      return card;
    }

    // Setup search functionality
    function setupBrandSearch(brands) {
      const searchInput = document.getElementById('brandSearch');
      const container = document.getElementById('brandsContainer');

      searchInput.addEventListener('input', function () {
        const searchTerm = this.value.toLowerCase();
        const cards = container.querySelectorAll('.brand-card');

        cards.forEach(card => {
          const text = card.textContent.toLowerCase();
          card.style.display = text.includes(searchTerm) ? 'block' : 'none';
        });
      });
    }

    function setupInfluencerSearch(influencers) {
      const searchInput = document.getElementById('influencerSearch');
      const container = document.getElementById('influencersContainer');

      searchInput.addEventListener('input', function () {
        const searchTerm = this.value.toLowerCase();
        const cards = container.querySelectorAll('.influencer-card');

        cards.forEach(card => {
          const text = card.textContent.toLowerCase();
          card.style.display = text.includes(searchTerm) ? 'block' : 'none';
        });
      });
    }
  </script>

</body>

</html>